#!/bin/bash

if $(tmux has-session -t main); then
    create_window='new-window ; '
else
    create_window=''
fi
tmux new-session -t main \; ${create_window} attach \; set-option destroy-unattached \; set-hook client-detached kill-window \; set-hook sessions-changed 'run-shell ~/bin/tmux-bg-status.sh' \; set-hook session-window-changed 'run-shell ~/bin/tmux-bg-status.sh'
